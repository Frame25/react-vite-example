import {Container} from 'shared/ui/container';
import {Input} from 'shared/ui/input';
import {Text} from 'shared/ui/text';

export const Invite = () => {
  return (
    <Container>
      <Text>Invite user by email</Text>
      <Input />
    </Container>
  );
};
